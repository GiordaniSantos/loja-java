spring:
  application:
    name: lojaInformatica
  datasource:
    url: jdbc:mysql://localhost:3306/loja-informatica
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
      show-sql: false
